name = "flashdrop-relay"
main = "src/index.js"
compatibility_date = "2024-09-23"

# 本地磁盘模式的中继 Worker：WebSocket 内存流转，不落盘。
# 部署后前端“本地直传”连 wss://<your-subdomain>.workers.dev/relay

[[durable_objects.bindings]]
name = "RELAY"
class_name = "Relay"

[[migrations]]
tag = "v1"
new_classes = ["Relay"]
