# 闪传 FlashDrop · 中转上传子系统 · 交接文档

> 用途：给后续研究者/优化者接手中转（tus）上传链路。本文档记录**当前架构、近期改动、已知瓶颈、以及"上传并行化"改造草案**。
> 最后更新：2026-08-03。前端当前已部署 `bc1e536`，Worker `conduit-tus` 同步部署。

---

## 0. 一句话现状

中转上传**功能正确、字节无损**（已真机端到端 SHA 校验闭环），但**上传是单连接顺序传，比下载慢**——这是明天要优化（上传并行化）的核心目标。跨境链路丢包 40.9% 是物理瓶颈，代码层只能靠"多车道并发"去榨带宽。

---

## 1. 架构全景（中转上传 → 下载 全链路）

```
发送端浏览器                         Cloudflare Worker(conduit-tus)              接收端浏览器
─────────────                       ─────────────────────────────              ─────────────
选文件                                  
  │                                  
  ├─ E2EE 开: encryptFile→密文Blob     
  │   (WebCrypto, 8MiB/块)            
  ▼                                  
tus.Upload(payload, {                POST /files ── createUpload
  chunkSize:8MiB,                      │  生成 fileId, 写 D1 files 行(offset=0)
  endpoint:/files })                   ▼
  │                                   R2: 每个 PATCH 存 {fileId}/part-{offset}
  ├─ PATCH /files/{fileId} ─────────►  (FixedLengthStream 包装, 流式不进内存)
  │   Upload-Offset / 8MiB chunk       └─ D1 updateOffset(fileId, newOffset)
  │   (★ 当前单连接顺序!)             
  └─ onSuccess ── 分享码 ?code=XXX     
                                      GET /download/{code}/{fileId}
                                         └─ list {fileId}/ 分片, 返回 manifest(JSON)
                                      前端 12 路并发拉 /part/{key} (每路 8MiB Range)
                                         └─ decryptBlob(WebCrypto) → a.click() 落盘
```

**关键不对称（瓶颈根因）**：下载侧 12 路并发已落地；**上传侧 tus 默认单连接顺序 PATCH**，没有并发补偿 → 同样跨境烂路，上传体感更慢。

---

## 2. 核心文件清单（路径 + 职责）

| 文件 | 位置 | 职责 | 谁改 |
|------|------|------|------|
| `src/composables/useTusUpload.ts` | 前端 | **上层编排**：`uploadOne`（单文件 tus 上传 + E2EE 加密）、`uploadAll`（文件级并发，默认 3）。已关 `findPreviousUploads` | 前端 |
| `src/crypto/tus-crypto.ts` | 前端 | E2EE 加解密：帧=`[4B明文长][4B密文长][16B IV][密文][32B HMAC]`；`E2EE_CHUNK_SIZE=8MiB`；`deriveKey`=PBKDF2(SHA-256,150000,salt) | 前端 |
| `src/transfer/tus/tus-handler.ts` | 前端 adjacent（平台无关协议层） | **tus 服务端协议处理器**：POST/PATCH/HEAD/DELETE/OPTIONS。PATCH 存 `{fileId}/part-{offset}`，offset 由 D1 记 | Worker 调用 |
| `src/transfer/tus/download-handler.ts` | 前端 adjacent | 下载：列分片 → 清单 JSON；`/part/{key}` 支持 Range 子范围 | Worker 调用 |
| `src/transfer/tus/tus-protocol.ts` | 前端 adjacent | CORS、`tusHeaders`、`parseMetadata`、`MAX_SIZE` | — |
| `conduit-tus/src/r2-storage.ts` | **仅 Worker**（用 `R2Bucket`） | 云端文件柜：put/get/delete/list，流式不缓冲 | Worker |
| `conduit-tus/src/d1-index.ts` | **仅 Worker**（用 `D1Database`） | 云端目录本：transfers/files/codes 表，offset 记录 | Worker |
| `conduit-tus/src/index.ts` | 仅 Worker | 路由：`/files`、`/api/transfer(s)`、`/download`、`/health`，scheduled sweep | Worker |
| `conduit-tus/src/factory.ts` | 仅 Worker | 绑定装配，缺 R2/D1 直接抛错（不回退本地） | Worker |

**分层铁律（勿破）**：依赖 `R2Bucket`/`D1Database` 等 Worker 全局类型的文件**只能放 `conduit-tus/src/`**；放 `src/transfer/tus/` 的必须是平台无关代码（前端 `vue-tsc` 会扫 `src/**`，混进 Worker 类型会构建失败）。

---

## 3. 近期改动（git log 摘要，重点标红）

| 提交 | 说明 | 关键点 |
|------|------|--------|
| `bc1e536` | **修复上传假完成**（★最重要） | 移除 `findPreviousUploads`/`resumeFromPreviousUpload`，加 `storeFingerprintForResuming:false`。根治"进度条完成但没上传到本传输" |
| `82b02f1` | 接收端优化 | 下载加 30s 超时重试、实时进度；接收区醒目边框；发送端补分享提示。**未碰上传逻辑** |
| `f88ad5e` | 块大小统一 | 上传切片/R2 存盘统一 8MiB；下载改 **12 路并发**拉 part（force push） |
| `2038d7f` | 下载防截断 | 分块从 30MB 降到 8MB + 重试；CF 截断受带宽/时长影响非固定字节 |
| `4ff3601` | 下载重构 | 分多次小块替代单响应，绕开 CF 截断 |
| `70a2e4d` / `47b96a8` | E2EE 加解密 | 加密切换 WebCrypto（~16MB/s→硬件级）；修大文件多块 IV/HMAC 两处 bug |
| `6569b54` / `956eaf6` | API 路径 | 中转请求统一改 **tus Worker 绝对地址**（`resolveTusBase()`），相对路径 `/files` 在 Pages 会被拦 405 |
| `a732b70` | offset 存储 | offset 从 R2 list 改 D1 记录，消除 O(n²) |
| `42fc839` | tus Worker 落地 | 自写最小端点（方案 B），复用 r2-storage/d1-index |

**块大小全景（中转全程 8MiB）**：加密块 `E2EE_CHUNK_SIZE`、上传 `chunkSize`、R2 存 `part`、下载 `SUB_CHUNK=8MiB`/并发 12、P2P 4MiB。

---

## 4. 已知瓶颈（带证据，非猜测）

1. **上传单连接 vs 下载 12 路**（代码层主因，可优化）
   - 下载：12 路并发把单连接 0.6 MB/s 榨到 1.8 MB/s（3×）。
   - 上传：tus 默认单连接顺序 PATCH，无任何并发兜底 → 同样跨境烂路体感更慢。

2. **跨境链路物理瓶颈**（不可靠代码解决，需绕行）
   - speedtest 实测：中国大陆 → 东京(NRT) 节点**丢包 40.9%**，延迟 92ms，下行 ~1.19 Mbps。
   - 上行走"出境好路"、下行走"入境烂路"，非对称。CF 截断、掉速都源于此。
   - R2 桶已落 APAC 区，存储侧够近；瓶颈在"中国→东京"入境拥堵。
   - **绕行手段**：① P2P 直连（字节不经 CF，仅受对端带宽 + libwebrtc ~26MB/s 天花板）；② 中转迁国内可达 CDN（阿里云 OSS / 腾讯云 COS / 七牛）。

---

## 5. 待做：上传并行化改造草案（明天的研究重点）

### 5.1 目标
让上传也像下载那样多车道并发，把单连接 ~0.6–1 MB/s 抬到接近下载的并发水平。**必须保持字节无损 + 与现有下载读取兼容**。

### 5.2 两条技术路线（推荐 A）

**路线 A：tus 原生 `parallelUploads` + 服务端 `Upload-Concat`（标准做法）**
- 前端：`tus.Upload` 加 `parallelUploads: 4~6`。客户端会把文件切成 N 段，每段作为独立 **partial** 上传，最后发一个 **final** 请求拼接。
- 服务端需新增：
  1. `OPTIONS` 声明 `Upload-Concat: partial`；
  2. `POST` 识别 `Upload-Concat: partial` → 建 partial 上传（独立临时 fileId，`{partialId}/part-0` 存 R2）；
  3. `POST` 识别 `Upload-Concat: final; <url1> <url2> ...` → **按列表顺序**读取各 partial R2 对象流拼接，写入最终 `{finalId}/part-0`（或按偏移顺序的多个 part），D1 设 `offset = 总长度`；
  4. partial 失败由 tus 重试；sweeper 须清理孤儿 partial。
- **兼容性**：最终密文仍落在 final 的 `{fileId}/part-*`（按 offset 排序即可），**下载侧 `download-handler.ts` 完全不用改**。
- 复杂度：中。需正确处理 final 拼接顺序、partial 原子性、过期清理。

**路线 B：客户端手动分片 + 复用现有 offset 存储（看似更简单，但有坑）**
- 思路：客户端把明文切 N 段、各自加密、各自作为独立 tus 上传到**同一 fileId 的不同 offset**。
- **致命坑**：现有 `PATCH` 要求 `offset === currentOffset` 且 D1 单调更新 —— **对同一 fileId 并发 PATCH 会产生 offset 竞争（race）**，导致 409 或错序。**不可直接并发到同一 fileId**。
- 结论：若要走 B，仍需"每段独立临时 fileId + 服务端拼接"，本质上退化为路线 A 的某个变体。所以**推荐直接做路线 A（Upload-Concat）**。

### 5.3 改造清单（路线 A）
- [ ] `tus-protocol.ts`：`OPTIONS` 响应加 `Upload-Concat: partial` 声明。
- [ ] `tus-handler.ts`：`createUpload` 区分 partial / final；新增 final 拼接逻辑（顺序读 partial → 流式拼 `finalId` → 写 R2 + D1）。
- [ ] `r2-storage.ts`：可能需要 `concat`/流式拷贝辅助（R2 不支持服务端拼接，需用 Worker 流式读了再写；注意 Worker CPU/内存限制，大文件要边读边写不整缓冲）。
- [ ] `useTusUpload.ts`：加 `parallelUploads` 选项；确认与 E2EE Blob 上传兼容（partial 的 payload 仍是加密 Blob）。
- [ ] `sweeper`：清理超时 partial。
- [ ] **验证**：见第 6 节；并行上传后接收端 SHA 必须与源文件逐字节一致；故障注入陈旧 `tus::` 条目下仍真实新建（不 resume）。

### 5.4 风险点
- R2 无服务端拼接，final 拼接要在 Worker 内流式完成，超大文件注意内存/CPU（可借 `FixedLengthStream` 边读边写）。
- partial 与 final 的过期时间需一致，避免 final 时 partial 已被 sweep。
- 上传中途断网：当前 `bc1e536` 已关客户端续传，断网会从头重传——并行化后每段独立续传，反而比现在更稳。

---

## 6. 验证方法与命令（三技能纪律）

> 严禁写 `.mjs/.sh` 测试脚本；一律 `agent-browser` 手动驱动双会话真机。临时 eval 载荷放 `test-assets/tmp/`、用完即删。

**agent-browser 启动参数（沙箱 Windows 铁证，否则 Chrome 崩溃挂死）：**
```
AGENT_BROWSER_ARGS="--no-sandbox --no-proxy-server --disable-gpu --disable-dev-shm-usage --disable-software-rasterizer"
```
注意：`open` 必须**直接带 URL**（`open https://...`），不带 URL 再 `navigate` 会挂；命令**不要接 `| tail`** 管道（常驻进程 EOF 等不到会永久挂起）。

**标准回归 SOP：**
1. 发送端：`open https://flashdrop.pages.dev/` → `upload input[type=file] <源文件>` → eval 点「开始传输」按钮 → 轮询快照看「已完成」。
2. 接收端：`open https://flashdrop.pages.dev/?code=XXX` → 填口令 → eval 点「解锁」→ 拦 `URL.createObjectURL` 捕获解密 Blob → 对 Blob 算 SHA-256 与源文件比对。
3. **字节级校验**：解密 Blob 的 SHA 必须 == 源文件 SHA（真机已验证 file1 `5066182c...`、file2 `a605eeff...` 全等）。
4. 故障注入：发送端 `localStorage.setItem('tus::<fp>::<id>', JSON.stringify({uploadUrl:'...STALE...'}))` 模拟陈旧续传缓存 → reload → 再传同大小不同内容文件 → 验证仍真实新建（分享码刷新、SHA 与新文件一致）。

**后端旁路核对（curl，路径别写错）：**
- 取传输信息：`GET https://flashdrop-tus.315461.xyz/api/transfer/{code}`（**单数** transfer）。
- 下载清单：`GET https://flashdrop-tus.315461.xyz/download/{code}/{fileId}`。

**权威校验脚本（网络慢时替代浏览器慢速下载）**：`test-assets/tmp/verify_decrypt.cjs` 按 tus-crypto 同款算法拉全部分片解密算 SHA（先用浏览器已验证的 file1 交叉校验实现正确性，再验新文件）。

---

## 7. 部署与分支约定

- 前端 `flashdrop.pages.dev`：push `main` 自动部署（已授权自动 push）。
- Worker `conduit-tus`（域名 `flashdrop-tus.315461.xyz`）：**仅改 `conduit/` 触发 `wrangler deploy`**；禁手动 deploy；`*.workers.dev` 在沙箱超时不可测（用户本机可访问）。
- 构建：`npm run build`（含 `vue-tsc`）前端必须通过；`conduit-tus` 有独立 `tsconfig.json` 完整类型检查。
- git：回退用 `git reset --hard`，**禁 `git revert`**；push 前 `git add -A`。

---

## 8. 关键坑速查（铁证，勿重蹈）

- **上传假完成根因**：tus 浏览器指纹 = `name+type+size+lastModified+endpoint`，**不含 transferId**；E2EE 时 payload 是普通 Blob（无 name/lastModified）指纹退化撞车 → `findPreviousUploads` 翻出旧 URL 直接 resume → HEAD 返 offset==size 立刻 onSuccess 不传数据。`bc1e536` 已修。
- **CF 截断非固定字节**：单请求过大（>~20–30MB 实测）被掐断，引导出"误导性完整性校验失败"。下载已用 8MiB 分块 + Range 重试规避；安全线 8MiB 远低于 20MB。
- **API 绝对地址**：中转请求前缀 `/files`、`/api`、`/download` 必须 `resolveTusBase()` 绝对地址，不能相对路径（Pages 不会路由到 Worker）。
- **PATCH 必须消费 body**：提前报错分支先 `request.body.cancel()`，否则 HTTP/3 多路复用下客户端卡死（commit `a1930e3`）。
- **FixedLengthStream**：R2 put 的流长度必须已知，否则报 "readable stream must have a known length"。
- **git push 代理**：沙箱 `NO_PROXY` 含 `127.*` 会绕过本机代理直连 GitHub 被 reset；用 `NO_PROXY= no_proxy= http_proxy=http://127.0.0.1:10808 https_proxy=http://127.0.0.1:10808 git push`。

---

## 9. 留给明天研究者的开放问题

1. 上传并行化选 A 还是先验证 B 的简化版？建议直接 A（Upload-Concat）。
2. R2 无服务端拼接，final 拼接在 Worker 内流式做，超大文件（>500MB）的 CPU/内存预算是否够？要不要限制单 final 拼接的并发读？
3. 是否要保留"会话内断点续传"（当前 `bc1e536` 为防假完成已关客户端续传，断网从头重传）？并行化后每段自带续传，可考虑有限恢复。
4. 是否把 P2P 直连作为对华默认首选链路，从根上绕开 CF 跨境管道？（P2P 不经 CF，仅受对端带宽 + libwebrtc ~26MB/s 天花板。）
