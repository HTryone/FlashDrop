# 闪传 中转上传 交接文档（瓶颈与方案）

> 版本锚点：c658b84（修复(中转上传UI): 流式上传进度反馈）
> 写于：2026-08-04
> 目的：把当前中转上传的瓶颈和待选方案交接清楚，避免重复走弯路。

---

## 一、当前版本状态（c658b84）

架构：浏览器本地加密 → 切 80MiB 网络块 → POST /api/presign 拿 R2 PUT 预签名 URL → 浏览器 fetch(PUT) 直传 R2 → POST /api/commit 二次确认。Worker 只发 URL、不碰字节流。

关键参数：
- src/composables/useTusUpload.ts：BLOCK = 80MiB、CONCURRENCY = 3、RETRIES = 5
- 进度反馈：用 TransformStream 包装 Blob.stream() 逐 chunk 累计已上行字节
- conduit-tus/wrangler.toml [vars]：R2 凭证（R2_ACCOUNT_ID / R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY）已写入，生产部署能读到
- 下载侧：按 part-偏移 多 key 重组（与上传块对齐）

已闭环的部分（不用再动）：
- presign 500 根因修复（Workers R2 binding 没有 createPresignedUrl，改用 aws4fetch 签名）
- 凭证注入（git 自动部署必须进 wrangler.toml [vars]，Dashboard 手填不可靠）
- 80MiB 落盘不再坏块（直传 R2 绕开旧 tus PATCH 穿 CF 边缘导致的字节损坏）

---

## 二、当前瓶颈（铁证，不是猜测）

瓶颈 = Cloudflare R2 边缘对单条 PUT 连接的约 60 秒传输硬超时。

证据链（用户实测 + 截图）：
1. 直传 R2 的部分块返回 403，响应头带 Server: cloudflare 和 Cf-Ray: ...-NRT → 请求确实到达 R2 边缘并被其处理（若连接在到达 CF 前被 reset，不会有 CF 响应头，只会 Failed to fetch）。
2. 失败请求的耗时规律集中在 59~60 秒 → 这是平台层对单条慢速连接的传输硬限制。
3. 部分块 200、部分块 403 → 排除 R2 签名/权限/CORS（若错应全败）；是逐块判定的，慢的块撞墙、快的块过。
4. 错误 XML 为 <Code>ExpiredRequest</Code> / Request has expired —— 这是连接被掐后 R2 的收尾拒绝，不是签名 URL 有效期到了（600/3600 秒对单块都绰绰有余）。

为什么旧并发雪上加霜：CONCURRENCY=3 + 失败重试开新连接（无 abort）→ 一被掐，多条连接同时重传同一批块 → 带宽被重复占用 → 更慢 → 更易撞 60s 墙 → 死循环。用户所见“一掐断就 3 个并发循环往复”即此。

---

## 三、已排除的原因（别再走这些弯路）

| 怀疑项 | 结论 | 依据 |
|---|---|---|
| 本地代理（Clash/V2Ray）伪造 403 | 否 | 用户明确确认“和代理没有任何关系”；响应头 Server: cloudflare 证明是 R2 边缘处理 |
| presigned URL 有效期太短（600s） | 否 | 600/3600 秒对单块上传绰绰有余；ExpiredRequest 是连接被掐后的收尾拒绝，非签名窗口过期 |
| R2 签名/权限/CORS 错误 | 否 | 部分块 200 证明全对 |
| 前端代码结构 | 否 | 已用 XHR / TransformStream 多种写法验证，问题在连接时长不在代码 |

---

## 四、需要的方案（待选，按推荐排序）

### 方案 A：动态时间切块（用户提出，保 80MiB）
- 思路：不按固定 80MiB 切，改按“连接还能活多久”切。在 50~55 秒主动结束当前连接、提前认领下一块，不等被边缘掐断。
- 做法：单块传输时计时，到 50s 主动切断当前连接、立即发起下一块；并发改为单发（1 路，用户指定）。
- 优点：保持 80MiB 量级、自适应网络快慢、不依赖任何平台参数。
- 代价：需要管理“已传字节偏移”、断点续传偏移对齐；计时切块的边界要和 commit 的 offset 对齐。
- 状态：已落地 2026-08-04（单发 + 50s 超时切小重传，不测速度不算块）。详见 useTusUpload.ts：BASE_SEG=80MiB 基准、SLICE_SECONDS=50、超时把 seg 减半重传同一 offset。

### 方案 B：Multipart Upload（R2 标准，治本）
- 思路：S3/R2 标准多片上传。把文件切 N 片，每片独立 PUT，最后 Complete 让 R2 在内部拼成单个完整对象。
- 单片尺寸窗口：最小 5MiB（最后一片可更小），最大 5GiB。我们选 5~10MiB/片。
- 落地步骤：
  1. 前端 → Worker POST /api/mpu/create → Worker 调 R2 CreateMultipartUpload 拿 UploadId
  2. 前端 → Worker POST /api/mpu/presign?uploadId=&partNumber=N → aws4fetch 签 UploadPart URL（每片独立）
  3. 前端 XHR → R2 直传每片（5~10MiB，几秒完，撞不到 60s 墙）
  4. 前端收集每片 R2 返回的 ETag
  5. 前端 → Worker POST /api/mpu/complete → 带 PartNumber+ETag 列表，R2 拼成单对象
- 优点：每片短连接彻底绕开 60s 墙（根治，非续命）；失败只重传该片；可并发；合并后下载侧可简化成单对象 Range 读。
- 代价：Worker 加 3 个端点；前端管 UploadId/PartNumber/ETag 收集/合并。
- 状态：aws4fetch 已能签这套 URL，R2 凭证已就绪，只差端点 + 前端改造。

### 方案 C：提速（治本但依赖外部）
- 思路：80MiB 能否在 60s 内传完，只看你到 R2（东京 NRT 边缘）的上行速率。80MiB ÷ 60s ≈ 14 Mbps 是生死线。
- 做法：换离用户近的 R2 区域 / 国内可达 CDN（阿里云 OSS / 腾讯云 COS / 七牛）。
- 状态：架构外，需用户决策是否上国内存储。

### 方案 D：缩块到 20MiB（治标）
- 做法：BLOCK 80→20MiB，每块在 60s 内轻松传完。
- 代价：请求数变 4 倍、速度略降。
- 状态：曾临时改过被撤，作为兜底保留。

---

## 五、待决策 / 待办

1. 选哪个方案：用户倾向“保 80MiB”，优先 A（动态时间切块）或 B（Multipart）。A 改动小、B 根治但改动大。
2. presign 有效期清理：r2-storage.ts 默认 3600，但 presign-handler.ts 调用方硬编码 expiresIn: 600 盖掉了默认 → URL 一直是 600。这不影响 60s 连接墙，但应统一（清理一致性）。
3. 并发数：用户最终意图是单发（1 路）——2026-08-04 已落地 useTusUpload.ts（移除 CONCURRENCY 常量，单 while 顺序传 + SLICE_SECONDS=50 超时切小重传）。
4. 密钥轮换：R2_SECRET_ACCESS_KEY 曾明文贴入聊天且入仓库，链路跑通后应去 R2 删旧建新并轮换。

---

## 六、验证方法（零代码，先钉死瓶颈）

- 传 5MB 小文件：应在 60s 内成功 → 证明签名/权限/CORS 全对。
- 传 80MB 大文件：必在 ~60s 被掐 → 坐实“连接时长问题，非签名/权限”。
- 二者二进制结果即定论，再决定上 A 还是 B。

---

## 附：关键记忆锚点

- 2a5454e wrangler.toml 凭证（git 部署必带）
- 50cc918 aws4fetch 替换不存在的 createPresignedUrl
- c658b84 当前版本（流式进度 + 80MiB + 并发3）
- 60s 墙根因：Cloudflare R2 边缘单连接传输硬超时（非代理/非 URL 过期/非代码）
